import DefaultTheme from 'vitepress/theme';
import type { Theme } from 'vitepress';
import './custom.css';

// Default VitePress theme plus the Stewie palette (see custom.css).
export default DefaultTheme satisfies Theme;
