{
  "name": "stewie-js-framework-benchmark",
  "version": "0.1.0",
  "private": true,
  "description": "Stewie implementation for js-framework-benchmark",
  "type": "module",
  "main": "dist/index.html",
  "scripts": {
    "build-prod": "vite build",
    "build": "vite build",
    "clean": "rm -rf dist",
    "nuke": "rm -rf dist node_modules",
    "dev": "vite --port 8080",
    "preview": "vite preview --port 8080"
  },
  "dependencies": {
    "@stewie-js/core": "workspace:*"
  },
  "devDependencies": {
    "@stewie-js/vite": "workspace:*",
    "vite": "^7.0.0"
  },
  "js-framework-benchmark": {
    "frameworkVersionFromPackage": "@stewie-js/core",
    "frameworkHomeURL": "https://github.com/stewiejs/stewie"
  }
}
