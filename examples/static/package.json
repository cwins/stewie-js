{
  "name": "static-example",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "nuke": "rm -rf dist node_modules",
    "build": "vite build",
    "dev": "vite",
    "test": "vitest run"
  },
  "dependencies": {
    "@stewie/core": "workspace:*",
    "@stewie/testing": "workspace:*",
    "vite": "^6.0.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
