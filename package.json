{
  "name": "stewie-js",
  "version": "0.0.1",
  "private": true,
  "description": "A TypeScript web rendering framework",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "changelog:root": "conventional-changelog -p angular -r 0 --pkg packages/core/package.json --commit-path packages/",
    "changelog:packages": "pnpm -r --filter='./packages/*' exec conventional-changelog -r 0 -p angular --commit-path .",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "check:edge": "node scripts/check-edge-packages.mjs",
    "lint": "oxlint packages/*/src examples/*/src --ignore-pattern '**/*.d.ts'",
    "lint:fix": "oxlint --fix packages/*/src examples/*/src --ignore-pattern '**/*.d.ts'",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "clean": "find packages -name dist -type d -prune -exec rm -rf {} + 2>/dev/null; find packages/*/src -name '*.js' -o -name '*.js.map' -o -name '*.d.ts' -o -name '*.d.ts.map' 2>/dev/null | xargs rm -f; true",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "conventional-changelog": "^7.2.0",
    "conventional-changelog-angular": "^8.3.1",
    "happy-dom": "^20.8.4",
    "husky": "^9.1.7",
    "oxfmt": "^0.43.0",
    "oxlint": "^1.56.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.9"
  },
  "packageManager": "pnpm@10.28.2"
}
